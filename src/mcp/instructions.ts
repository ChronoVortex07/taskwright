/**
 * Workflow guidance sent during MCP initialization. Keep the essential loop in
 * the first 512 characters because clients may truncate server instructions.
 */
export const TASKWRIGHT_MCP_INSTRUCTIONS =
  'Taskwright manages work through its MCP tools. At session start call get_active_task, then claim_task with the assigned task ID — both it and start_task RETURN the task\'s full context, so work from that. Work only in the isolated task worktree. Record progress and final notes with edit_task. When the worktree is committed and clean, close with request_merge and wait for its result — it marks the task Done on the board, and you do not merge from the primary checkout yourself. A branch with NO board task (a dev or scratch worktree) closes the same way, through request_branch_merge — never with a manual git merge in the repo root. Because start_task and claim_task hand back the description, acceptance criteria, plan and board file path, you never need to search the filesystem for the board — in git-auto mode it does not live under the repo root at all. get_active_task reports its source: "marker" (board/dispatch), "session" (the task this session itself started/claimed), or "none" — and when the session has several tasks in flight it returns candidates instead of guessing, because an MCP call carries no working directory. If it has no assignment, ask which task to work on ONLY when the user asked you to work on a board task without naming one; for a standalone request like a code review or a question, just do what was asked without an active task.';
