node_modules/
dist/
out/
*.vsix
.vscode-test/
**/.DS_Store

# vscode-extension-tester artifacts
/core
.claude/settings.local.json
tmp/
test-results/

# Playwright visual-proof screenshots (regenerated by `bun run proof`)
e2e/__screenshots__/

# https://github.com/obra/superpowers/blob/main/skills/using-git-worktrees/SKILL.md
.worktrees/

# Taskwright local session state (active task is per-checkout, not shared)
.taskwright/

# Lockfiles from non-Bun package managers. This project uses bun.lock;
# block accidental commits if someone runs npm/yarn/pnpm install.
package-lock.json
yarn.lock
pnpm-lock.yaml
# >>> taskwright synced board >>>
# Board tasks live on the taskwright-board ref, not on code branches.
backlog/tasks/
backlog/drafts/
backlog/completed/
backlog/archive/
# <<< taskwright synced board <<<
